<template>
  <view class="example example--wrap">
    <!-- head -->
    <view wx:if="{{title || desc}}" class="example__head">
      <view wx:if="{{title}}" class="example__head--title">{{title}}</view>
      <view wx:if="{{title && desc}}" class="example__head--line"></view>
      <view wx:if="{{desc}}" class="example__head--desc">{{desc}}</view>
    </view>

    <slot></slot>

    <!-- foot -->
    <!-- <navigator class="example__foot" openType="switchTab" url="/pages/component/index" hover-class="none">
      <image class="example__foot--icon" src="https://s10.mogucdn.com/mlcdn/c45406/170913_0e8g7dl1f2l75a8h6he12436a8024_152x23.png"></image>
    </navigator> -->
  </view>
</template>

<script>
export default {
  properties: {
    title: {
      type: String,
      value: ''
    },
    desc: {
      type: String,
      value: ''
    }
  }
}
</script>

<style lang="pcss">
@b example {
  @m wrap {
    padding: 0 10rpx;
  }
  @e head{
    padding: 30rpx 30rpx 0;
    text-align: center;
    @m title{
      display: inline-block;
      padding: 0 40rpx 10rpx 40rpx;
      font-size: 32rpx;
      color: #9B9B9B;
    }
    @m line{
      margin: 0 auto;
      width: 150rpx;
      height: 2rpx;
      background-color: #D8D8D8;
    }
    @m desc{
      padding-top: 13rpx;
      color: #BEBEBE;
      font-size: 26rpx;
    }
  }
  @e foot{
    margin: 100rpx 0 30rpx 0;
    text-align: center;
    color: #1aad19;
    font-size: 0;
    @m icon{
      width: 152rpx;
      height: 23rpx;
    }
  }
}
</style>

