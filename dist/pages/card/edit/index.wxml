
  <view class="header-box"></view>

  <view class="show-box">
    <swiper class="show-swiper"
      indicator-dots="{{indicatorDots}}"
      autoplay="{{autoplay}}"
      interval="{{interval}}"
      duration="{{duration}}"
      vertical="{{vertical}}"
    >
      <form bindsubmit="submitForm" report-submit="true">
        <swiper-item class="show-swiper-item">
          <input class="weui-input" placeholder="聚会主题1" name="title1"/>
        </swiper-item>
        <swiper-item class="show-swiper-item">
          <input class="weui-input" placeholder="聚会主题2" name="title2"/>
        </swiper-item>

      </form>

    </swiper>
  </view>
  <view class="submit-box" hover-class="submit-btn_active">
      <view class="submit-btn">保存</view>
  </view>
