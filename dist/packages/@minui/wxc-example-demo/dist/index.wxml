<view class="example--box">
    <view class="example--demo demo">
        <view class="demo-inner">
            <slot></slot>
            <view class="countdown">
              <block wx:if="{{countdown}}">
                <wxc-label class="label_countdown" type="fill" type-color="#ff4444">
                  <wxc-countdown countdown="{{countdown}}"></wxc-countdown>
                </wxc-label>
              </block>
              <wxc-label class="label_coming" type="fill" type-color="#ffe8ee" text-color="#ff5577">{{coming}}人报名</wxc-label>
            </view>
            <view wx:if="{{image_url == ''}}">
              <image class="dome-image" src="/common/assets/images/activity_bg.jpg" mode="aspectFill"></image>
            </view>
            <view wx:else>
              <image class="dome-image" src="{{image_url}}" mode="aspectFill"></image>
            </view>
        </view>
    </view>
    <view class="example--meta meta">
        <view class="meta-title">{{title}}</view>
        <view class="meta-desc">
          <image class="meta-icon" src="../../../../common/assets/images/position.png"></image>
          <navigator class="meta-navigator" url="/pages/navigation/index?latitude={{latitude}}&longitude={{longitude}}" hover-class="navigator-hover">
            位置：{{desc}}
          </navigator>
        </view>
        
    </view>
    <view class="example--source">
        <view class="view-btn {{viewSourceClass}}" bindtap="bindViewSourceEvent">{{ isExpandSource ? '收起列表' : '查看详情' }}</view>
        <view wx:if="{{isExpandSource}}">
          <block wx:for="{{avatar}}" wx:key="item">
            <block wx:if="{{item.is_coming}}">
              <wxc-avatar class="avatar" count="{{item.count}}" src="{{item.user.avatar_url}}"></wxc-avatar>
            </block>
          </block>
          <view class="button-sp-area">
            <block wx:for="{{avatar}}" wx:key="item">
              <block wx:if="{{item.is_master == 1}}">
                  <navigator url="/pages/activity-detail/index?user_id={{item.user_id}}&activity_id={{item.activity_id}}">
                    <button class="weui-btn mini-btn" type="warn" plain="true" size="mini">进入聚会圈</button>
                  </navigator>
              </block>
            </block>
          </view>
        </view>
    </view>
</view>